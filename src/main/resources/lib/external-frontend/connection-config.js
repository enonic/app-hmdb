exports.FRONTEND_ORIGIN = "http://localhost:3000"                                  // <- hardcode for poc.

// URI request parameter added by the proxy to flag to next.js that the request is coming from the XP preview
exports.FROM_XP_PARAM = '__fromXp__';
